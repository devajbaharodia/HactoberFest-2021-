#include <stdio.h>

int main()
{
    float s, area;
    printf("Enter the side of the square ");
    scanf("%f", &s);
    area = s*s;
    printf("The area of the square is %f", area);
    return 0;
}
